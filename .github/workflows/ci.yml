name: CI

on:
  push:
    branches: [ "main" ]
    tags: [ 'v*.*.*' ]
  pull_request:

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*','**/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      - name: Build on Unix (macOS / Linux)
        if: runner.os != 'Windows'
        run: |
          if [ ! -f ./gradlew ]; then gradle wrapper; fi
          chmod +x gradlew
          ./gradlew :core:build --no-daemon
        shell: bash

      - name: Build on Windows
        if: runner.os == 'Windows'
        run: |
          if (Test-Path .\gradlew.bat) { .\gradlew.bat :core:build --no-daemon } else { gradle wrapper; .\gradlew.bat :core:build --no-daemon }
        shell: pwsh

  smoke-tests:
    needs: build
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Run automated CI smoke tests
        shell: pwsh
        run: |
          powershell -ExecutionPolicy Bypass -File scripts\ci-test.ps1 -Mode quick -ExampleTimeoutSec 45

  publish:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build, smoke-tests]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Publish to GitHub Packages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ ! -f ./gradlew ]; then gradle wrapper; fi
          chmod +x gradlew
          ./gradlew :core:publish --no-daemon
        shell: bash
